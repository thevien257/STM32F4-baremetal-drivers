#include <stdint.h>
#include <main.h>

GPIO_Handle_TypeDef gpio_handle;

void GPIO_USER_INIT(void);

int main(void) {

	/* Loop forever */
	for (;;)
		;
}

void GPIO_USER_INIT(void) {
	gpio_handle.GPIOX = GPIOA;
	gpio_handle.mode = GPIO_Output_Mode;
	gpio_handle.output_speed = GPIO_SPEED_VERY_HIGH;
}
